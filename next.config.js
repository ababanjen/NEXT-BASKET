/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["https://i.dummyjson.com", "https://dummyjson.com"],
  },
};

module.exports = nextConfig;
